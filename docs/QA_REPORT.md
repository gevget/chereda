# QA report · 18.09.2026

## Выполненные проверки

| Проверка | Результат |
|---|---|
| `pnpm lint` | успешно, без предупреждений после очистки импортов и конфигурации локальных SVG |
| `pnpm typecheck` | успешно, TypeScript strict |
| `pnpm test` | 8/8 проверок seed и связей |
| `pnpm build` | успешно, Next.js production build |
| `pnpm test:e2e` | 4/4 investor-сценария |
| Локальный сервер | `http://127.0.0.1:3000`, проверен в браузере |

## Сценарии

1. Лента → «Белый сад» → три перехода в media viewer клавиатурой → 10 участников → профиль Елены → вкладка услуг → локальная заявка: пройден.
2. PRO → Москва, 80–150 гостей, до 500 000 ₽, сторонний кейтеринг → Лофт «Берег» → рейтинг категории → score breakdown: пройден.
3. Проект → новая коллекция → открыть из toast → смена города → тёмная тема → refresh: пройден.
4. «О Chereda» → пример проекта → CTA в PRO: пройден.

## Визуальная и маршрутизаторная проверка

- `/feed`, `/projects/white-garden-wedding`, `/profiles/elena-sokolova-hair`, `/pro/venues` и `/ratings` проверены в браузере на 1920×1080, 1440×900, 1280×800, 1024×768, 768×1024, 390×844 и 360×800: заголовок и контент видны, горизонтального переполнения и Next error overlay нет.
- На 390×844 дополнительно открыты `/search`, `/pro`, `/pro/beauty`, `/collections`, `/collections/wedding-shortlist`, `/me`, `/notifications`, `/settings`, `/about`, `/demo`, а также loading/error state ленты: все открываются без переполнения.
- Light и dark визуально осмотрены; dark сохраняется после refresh. После исправления загрузки темы новая вкладка не показала ошибок консоли.
- После сверки с `макеты/*.svg` и PDF повторно проверены ключевые композиции: центрированная шапка, visual masonry в ленте, серый search hero, верхняя таксономия PRO и переключение вида карточек `standard / compact / visual`. Roboto Flex Variable загружается локально.
- `/pro` дополнительно сверена с листом «PRO»: выбранная категория «Все», верхняя сетка направлений, блок «Популярные / Площадки», горизонтальные showcase-карточки с четырьмя превью и CTA в фильтры. `/pro/venues` сохранён как рабочая фильтруемая категория.
- В ленте истории приведены к исходной композиции: синее кольцо вокруг первого изображения и три узких превью рядом; masonry оставлена визуальной, с теми же крупными скруглениями и без постоянной панели текста.
- В поиске hero начинается сразу под шапкой, имеет серый фон и центрированный заголовок «Поиск Chereda»; поле запроса, фильтры и сортировка выстроены в одну верхнюю строку, а выдача использует image-only карточки с шестиколоночной desktop-сеткой.
- Фильтры поиска переведены в макетные плашки без внешних подписей: «Введите запрос», «Область творчества», «Расположение», «Все стили», «Рекомендуемые». Для выбранных значений показываются счётчик, отдельная кнопка удаления и URL-состояние; стили поддерживают множественный выбор. Добавлен блок «Предложения» с визуальными тегами и быстрыми чипами.
- Дополнительная сверка UI kit: меню области творчества открывается под своей плашкой, поисковые теги используют радиус 12 px и шрифт 14 px, вкладки ленты «Для вас / Популярное / Новое» имеют высоту 36 px и серый фон, элементы шапки — высоту 36 px.
- Проверены keyboard arrows/Escape в media viewer, фокус в contact modal, подписи основных icon-only кнопок, skip link. Формальный аудит контраста WCAG AA и полный аудит клавиатуры всех фильтров не проводились.

## Ограничения относительно полного Definition of Done

- `public/media` содержит 240 уникальных локальных нейтральных SVG-композиций и 72 avatar/cover пары. Это честно обозначенные demo-визуалы, но не готовая stock-фотоподборка по `06_MEDIA_AND_STOCK_RULES.md`. Официальная лицензия Pexels проверена, однако прямая загрузка с CDN в workspace закончилась ошибкой аутентификации. План лицензированной замены — `docs/MEDIA_TODO.md`.
- Реализованы необходимые для investor-сценария и несколько универсальных URL-фильтров: город, категория/специализация, вместимость, бюджет, сторонний кейтеринг, рейтинг, проверка, выезд и стиль. Полный перечень отраслевых полей из `04_FILTER_CATALOG.md` ещё не реализован. Не считать каталог полностью принятым.
- Loading/empty/error доступны как явные симуляции через `/demo`; реальные списки имеют populated/no-results. Полная матрица состояний каждого маршрута и формальный pairwise accessibility тест пока не завершены.
- Заявки, рейтинг, уведомления и сохранения — локальная demo-симуляция без отправки, геокодирования и backend.

Эти ограничения не скрыты в интерфейсе и README. Публикация не выполнялась.
- Финальная типографическая сверка: базовый текст 14/20, body large 16/24, заголовки 36/42 и 24/30 на desktop, 28/34 и 20/26 на mobile; все страницы используют локальный Roboto Flex Variable.
- Внутренний ритм нормализован токенами: отступ страницы 40/24, секций 40, заголовка секции 16, карточек 16; рейтинг получил выровненные карточки Top-3 и строки списка с единым вертикальным центром.
- Footer отделён от контента верхним разделителем и равномерным интервалом ссылок; ссылки больше не прилипают к карточкам и сохраняют line-height 20 px.
- Переключатель темы в шапке теперь сразу меняет `data-theme` и CSS-переменные (light ↔ dark); выбор «Как в системе» в настройках сохранён. Проверено на `/settings` и `/ratings`, включая переход между страницами.
- PRO-каталог дополнен тремя вариантами карточек по листу `про с фильтрами светлая разные варианты карточек.pdf`: стандартный 3-колоночный, компактный 4-колоночный с сеткой 3×3 и крупный 2-колоночный с широкой галереей. Во всех вариантах видны имя, специализация, город, цена, рейтинг, отзывы и число проектов.
- Горизонтальная подборка «Популярные» получила настоящие стрелки предыдущей/следующей страницы, плавную прокрутку и защиту от горизонтального переполнения окна. Сортировка и вид карточек сохраняются в URL (`sort`, `view`), фильтры каталога продолжают обновлять выдачу.
- Верхние story-плашки ленты приведены к макету `Главная.pdf`: синий контур вокруг аватара, три широких превью, единая pill-форма и адаптивная мобильная версия.
- `/about` полностью переработан как investor/partner landing внутри App Shell: 9 смысловых блоков от Hero до финального CTA, интерактивный preview проекта «Белый сад» и список связанных credits.
- В шапку добавлен пункт «О проекте». CTA ведут в `/feed`, `/search`, `/pro`, проект «Белый сад» и profile routes. Партнёрский CTA открывает локальный demo-modal с выбором типа обращения и сохраняет заявку в `localStorage` с честной маркировкой demo.
- На `/about` проверены светлая тема, якорная навигация «Как это работает», открытие проекта, переход в PRO, partner contact flow и отсутствие ошибок браузера. Формулировки про отличие и roadmap оставлены осторожными и не выдают будущие возможности за текущие.
- `/ratings` переработан в полноценный leaderboard: объяснение score и кнопка методики, URL-фильтры категории/города/периода, контекст выбранного среза, Top‑3 с verified, score, отзывами и переходом в профиль, полный список с динамикой и ссылками на отзывы.
- Добавлен визуальный блок «Как считается рейтинг» с пятью факторами, значениями, progress-индикаторами, датой и явной маркировкой demo-методики. Score dialog дополнен ссылкой «Подробнее о методике».
- Для рейтинга проверены фильтры, empty state другого города, совместное применение параметров, открытие score breakdown, переходы в профиль и отзывы, keyboard Enter на строке и отсутствие console errors.


## Финальный release audit — 19.09.2026

- Карта сайта дополнена тарифами и публичным Partner Hub; динамические сущности представлены по одному заполненному типу.
- Контекстные входы проверены: project → brief/room, collection → brief/board, PRO → crew/requests/venue fit, profile → credits, partner preview → public hub, Radar → partner/catalog.
- Добавлен E2E growth walkthrough и мобильный overflow-check всех десяти capability-страниц.
- Добавлены unit-тесты связности Partner Hub, explainable team score, Venue Fit warnings и детерминированных Radar-срезов.
- Рекламная позиция PRO маркируется текстом и золотой рамкой постоянно; она не зависит от hover.
- Demo-ограничения: backend, auth, платежи, юридическая верификация, availability, отправка заявок и ML не подключены.


## Аудит ноутбучной сетки и заполненности — 19 сентября 2026

- Общие карточные сетки используют читаемую минимальную ширину и автоматически переходят с четырёх колонок на три или две.
- На ширине 900 px карточки команды проекта отображаются по две в ряд, а топ рейтинга — в две колонки.
- Поиск на ширинах 768–1023 px использует три колонки; мобильная выдача сохраняет две визуальные колонки.
- Действия проекта на мобильном экране перестраиваются в сетку 2 × 2; медиалента прокручивается внутри контейнера без горизонтального скролла страницы.
- Внутренние отступы карточек выровнены, компактные элементы и факты используют скругление 12–16 px.
- Карта типов страниц проверена на ширинах 900 и 390 px: каждый экран содержит основной заголовок и заполненный контент, горизонтальное переполнение отсутствует.
- Добавлены E2E-регрессии на минимальную ширину карточек проекта, рейтинга и поиска, а также на все маршруты карты сайта.
- Проверки: typecheck, lint, 13 unit-тестов, production build на 280 статических страниц, 10 E2E-сценариев.
